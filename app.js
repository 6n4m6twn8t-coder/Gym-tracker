const PROGRAM={Push:[['Incline Dumbbell Press','Upper chest','Incline Machine Press|Incline Smith Press|Low-to-High Cable Press'],['Flat Dumbbell Press','Chest','Chest Press Machine|Smith Bench Press|Push-Up'],['Dumbbell Lateral Raise','Side delts','Cable Lateral Raise|Lateral Raise Machine'],['Rope Triceps Pushdown','Triceps','Straight-Bar Pushdown|Single-Arm Cable Extension'],['Cable Fly','Chest','Pec Deck|Dumbbell Fly'],['Cable Crunch','Abs','Machine Crunch|Weighted Crunch']],Pull:[['Lat Pulldown','Lats','Assisted Pull-Up|Neutral-Grip Pulldown'],['Chest-Supported Row','Mid back','Seated Cable Row|Machine Row'],['Single-Arm Cable Row','Lats','Dumbbell Row|Machine Row'],['Face Pull','Rear delts','Reverse Pec Deck|Rear-Delt Cable Fly'],['Dumbbell Curl','Biceps','Cable Curl|Machine Curl'],['Hammer Curl','Biceps / brachialis','Rope Hammer Curl|Cross-Body Curl']],Legs:[['Leg Press','Quads / glutes','Hack Squat|Goblet Squat'],['Romanian Deadlift','Hamstrings / glutes','Seated Leg Curl|45° Back Extension'],['Leg Extension','Quads','Sissy Squat|Split Squat'],['Seated Leg Curl','Hamstrings','Lying Leg Curl|Romanian Deadlift'],['Standing Calf Raise','Calves','Seated Calf Raise|Leg Press Calf Raise'],['Hanging Knee Raise','Abs','Captain’s Chair Raise|Reverse Crunch']]};const $=s=>document.querySelector(s),app=$('#app');const load=(k,d)=>{try{return JSON.parse(localStorage.getItem(k))??d}catch{return d}},save=(k,v)=>localStorage.setItem(k,JSON.stringify(v));let active=load('pa_active',null),history=load('pa_history',[]),tick;document.querySelectorAll('[data-nav]').forEach(b=>b.onclick=()=>render(b.dataset.nav));function render(page='today'){clearInterval(tick);if(active&&page==='today')return workout();if(page==='history')return historyPage();if(page==='library')return library();app.innerHTML=`<div class="card"><h2>Today</h2><p class="muted">Choose the session that fits today's training.</p></div>${Object.keys(PROGRAM).map(n=>`<button class="card workout" data-start="${n}"><h2>${n}</h2><span class="muted">${PROGRAM[n].length} exercises · ~45 min</span></button>`).join('')}`;document.querySelectorAll('[data-start]').forEach(b=>b.onclick=()=>start(b.dataset.start))}function start(name){active={id:Date.now(),name,started:Date.now(),notes:'',exercises:PROGRAM[name].map(([name,target,alts])=>({name,target,alts:alts.split('|'),sets:[{w:'',r:'',rir:''},{w:'',r:'',rir:''},{w:'',r:'',rir:''}]}))};save('pa_active',active);workout()}function workout(){app.innerHTML=`<div class="timer"><b>${active.name}</b><span id="clock">00:00</span></div><div class="card"><span class="tiny">ACTIVE SESSION</span><h2>${active.name}</h2><p class="muted">Last performance appears when this exercise has history.</p>${active.exercises.map((e,i)=>exerciseHTML(e,i)).join('')}</div><div class="card"><h3>Session note</h3><textarea id="note" placeholder="Anything worth remembering…">${active.notes||''}</textarea></div><button class="primary" id="finish">Finish & save workout</button> <button id="cancel" class="danger">Cancel</button>`;$('#note').oninput=e=>{active.notes=e.target.value;persist()};document.querySelectorAll('[data-set]').forEach(el=>el.onchange=setChange);document.querySelectorAll('[data-swap]').forEach(b=>b.onclick=()=>swap(+b.dataset.swap));$('#finish').onclick=finish;$('#cancel').onclick=()=>{if(confirm('Cancel this workout?')){active=null;localStorage.removeItem('pa_active');render()}};clock();tick=setInterval(clock,1000)}function exerciseHTML(e,i){let last=findLast(e.name);return `<div class="exercise"><div class="row"><div class="grow"><b>${e.name}</b><div class="tiny">Target · ${e.target}</div>${last?`<div class="tiny">Last · ${last}</div>`:''}</div><button class="swap" data-swap="${i}">Swap</button></div><div class="sets"><span class="tiny">SET</span><span class="tiny">KG</span><span class="tiny">REPS</span><span class="tiny">RIR</span>${e.sets.map((s,j)=>`<span>${j+1}</span><input inputmode="decimal" data-set="${i},${j},w" value="${s.w}"><input inputmode="numeric" data-set="${i},${j},r" value="${s.r}"><input inputmode="numeric" data-set="${i},${j},rir" value="${s.rir}" placeholder="–">`).join('')}</div></div>`}function setChange(ev){let [i,j,k]=ev.target.dataset.set.split(',');active.exercises[i].sets[j][k]=ev.target.value;persist()}function persist(){save('pa_active',active)}function swap(i){let e=active.exercises[i],choices=e.alts.map((x,n)=>`${n+1}. ${x}`).join('\n'),n=prompt(`Swap ${e.name} for:\n${choices}\n\nEnter number:`),pick=e.alts[+n-1];if(!pick)return;e.alts=[e.name,...e.alts.filter(x=>x!==pick)];e.name=pick;persist();workout()}function clock(){let s=Math.floor((Date.now()-active.started)/1000),m=Math.floor(s/60);$('#clock').textContent=`${String(m).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`}function finish(){active.finished=Date.now();history.unshift(active);save('pa_history',history);localStorage.removeItem('pa_active');active=null;historyPage()}function findLast(name){for(let h of history){let e=h.exercises.find(x=>x.name===name);if(e){let s=e.sets.filter(x=>x.w&&x.r).map(x=>`${x.w}×${x.r}`);if(s.length)return s.join(', ')}}return''}function historyPage(){app.innerHTML=`<div class="card"><h2>History</h2><p class="muted">${history.length?history.length+' saved sessions':'No sessions yet.'}</p></div>${history.map((h,i)=>`<button class="card workout" data-h="${i}"><h3>${h.name}</h3><span class="muted">${new Date(h.started).toLocaleString()}</span></button>`).join('')}`;document.querySelectorAll('[data-h]').forEach(b=>b.onclick=()=>detail(+b.dataset.h))}function detail(i){let h=history[i],mins=Math.round((h.finished-h.started)/60000);app.innerHTML=`<div class="card"><h2>${h.name}</h2><p class="muted">${new Date(h.started).toLocaleString()} · ${mins} min</p>${h.exercises.map(e=>`<div class="exercise"><b>${e.name}</b><div>${e.sets.filter(s=>s.w&&s.r).map(s=>`${s.w} kg × ${s.r}${s.rir!==''?` · RIR ${s.rir}`:''}`).join('<br>')||'No completed sets'}</div></div>`).join('')}${h.notes?`<p>${h.notes}</p>`:''}</div><button id="share">Share with Atlas</button> <button id="del" class="danger">Delete session</button>`;$('#share').onclick=()=>navigator.clipboard?.writeText(summary(h)).then(()=>alert('Session copied. Paste it into Atlas.'));$('#del').onclick=()=>{if(confirm('Delete this session?')){history.splice(i,1);save('pa_history',history);historyPage()}}}function summary(h){return `${h.name} — ${new Date(h.started).toLocaleString()}\n`+h.exercises.map(e=>`${e.name}: `+e.sets.filter(s=>s.w&&s.r).map(s=>`${s.w} kg × ${s.r}${s.rir!==''?` (RIR ${s.rir})`:''}`).join(', ')).join('\n')+(h.notes?`\nNotes: ${h.notes}`:'')}function library(){app.innerHTML=`<div class="card"><h2>Exercise Library</h2><p class="muted">Anatomy thumbnails and movement demos plug into this layer after the core tracker passes iPhone testing.</p></div>${Object.entries(PROGRAM).map(([g,es])=>`<div class="card"><h3>${g}</h3>${es.map(e=>`<div class="exercise"><b>${e[0]}</b><div class="tiny">${e[1]}</div><div>${e[2].split('|').map(a=>`<span class="tag">${a}</span>`).join('')}</div></div>`).join('')}</div>`).join('')}`}render();